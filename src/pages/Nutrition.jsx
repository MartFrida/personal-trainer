import { theme } from '../helpers/theme';

export const Nutrition = () => {
  return (
    <div className={`${theme.background} p-6 ${theme.text}`}>Personalized meal plans and dietary advice.
      <p>Dieta, Nutricion</p></div>
  )
}
