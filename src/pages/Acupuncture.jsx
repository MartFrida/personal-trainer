import { theme } from '../helpers/theme';

export const Acupuncture = () => {
  return (
    <div className={`${theme.background} p-6 ${theme.text}`}>Holistic treatments for overall wellness.</div>
  )
}
